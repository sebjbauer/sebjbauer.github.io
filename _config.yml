# Files that stay in the repository but are not published on the website
exclude:
  - FEATURES.md
  - README.md
  - cv          # the CV source; the compiled PDF is published as /cv.pdf by the workflow
